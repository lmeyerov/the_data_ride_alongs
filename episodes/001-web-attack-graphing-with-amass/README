## Notebook

* Run in your favorite notebook environment such as Jupyter or Google Colab

## Amass

* See (and even run!) instructions within notebook for installing Amass

## Neo4j instructions

* Requires docker / docker-compose
* Run with `neo4j $ docker-compose up -d` or `neo4j $ sudo docker-compose up`
